<p style="color: green"><%= notice %></p>
<%= button_to(
        "Log Out",
        destroy_user_session_path,
        method: :delete
      ) %>



  
  <h1>Books</h1>
  <div id="books">
  <% @books.each do |book| %>
    <%= turbo_frame_tag "book_#{book.id}" do %>
      <%= render book %>
      <p>
        <%= link_to "Show this book", book %>
        <%= button_to "Destroy this book", book, method: :delete %>
      </p>
    <% end %>
  <% end %>
  </div>




<%= link_to "New book", new_book_path, data: { turbo_frame: dom_id(Book.new) } %>

<%= turbo_frame_tag dom_id(Book.new) do %>
<% end %>
